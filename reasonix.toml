# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/AppData/Roaming/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 3   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
# max_steps         = 0   # executor tool-call rounds; 0 = no limit
# planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# reasoning_language = "zh"   # visible reasoning language: auto|zh|en
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# keep                = ["errors"]   # compaction keep policy: errors, user_marked
# recent_keep         = 2   # minimum recent messages kept verbatim
cold_resume_prune   = true   # elide stale tool results when reopening a session past the provider cache window
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[tools.background_jobs]
stalled_warning_seconds = 900   # warn once per background job after this many quiet seconds; 0 disables

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["Edit", "Bash(git --version:*)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git init)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && copy AGENTS.md CLAUDE.md)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && cp AGENTS.md CLAUDE.md)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git add .)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git commit -m \"初始化：AGENTS.md + reasonix 项目配置\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git config user.email \"you@example.com\" && git config user.name \"垂杨柳等评\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git remote add origin https://github.com/15831290932-a11y/zhibiao--evaluation.git && git branch -M main)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git push -u origin main)", "Bash(git config --global --list 2>&1 | findstr proxy)", "Bash(curl -s --connect-timeout 5 https://github.com -o /dev/null -w \"%{http_code}\" 2>&1 || echo \"无法连接\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git remote set-url origin https://gitee.com/15831290932-a11y/zhibiao-evaluation.git)", "Bash(findstr /M \"port\" \"%USERPROFILE%\\.config\\clash\\config.yaml\" 2>nul || findstr /M \"port\" \"%USERPROFILE%\\AppData\\Local\\Clash*\\config.yaml\" 2>nul || echo \"未找到 Clash 配置\")", "Bash(tasklist /FI \"IMAGENAME eq clash*\" 2>nul || echo \"未找到 clash 进程\")", "Bash(tasklist /FI \"IMAGENAME eq clash-verge*\" 2>nul & tasklist /FI \"IMAGENAME eq clash*\" 2>nul & tasklist /FI \"IMAGENAME eq cfw*\" 2>nul & netsh winhttp show proxy 2>nul || echo \"done\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git config --global http.proxy http://127.0.0.1:7897 && git config --global https.proxy http://127.0.0.1:7897)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git remote set-url origin https://github.com/15831290932-a11y/zhibiao--evaluation.git && git push -u origin main)", "Bash(curl -x http://127.0.0.1:7897 -s --connect-timeout 5 https://www.baidu.com -o /dev/null -w \"%{http_code}\" 2>&1)", "Bash(curl -x http://127.0.0.1:7897 -s --connect-timeout 10 https://github.com -o /dev/null -w \"%{http_code}\" 2>&1)", "Bash(curl -x http://127.0.0.1:7897 -s --connect-timeout 10 https://www.google.com -o /dev/null -w \"%{http_code}\" 2>&1)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git config --global http.version HTTP/1.1 && git config --global http.postBuffer 524288000 && git push -u origin main)", "Bash(curl -x http://127.0.0.1:7897 -s --connect-timeout 15 https://github.com 2>&1 | head -5)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && https_proxy=http://127.0.0.1:7897 http_proxy=http://127.0.0.1:7897 git push -u origin main)", "Bash(git config --list --show-origin 2>&1 | findstr \"http ssl proxy\")", "Bash(git config --global http.sslbackend schannel && cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git push -u origin main)", "Bash(curl -s --connect-timeout 10 https://github.com -o /dev/null -w \"%{http_code}\" 2>&1)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git log --oneline)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && mkdir -p \"docs/已完成指标\" && mkdir -p \"docs/已完成指标/第一章\" && mkdir -p \"docs/已完成指标/第二章\" && mkdir -p \"docs/已完成指标/第三章\" && mkdir -p \"docs/已完成指标/第四章\" && mkdir -p \"docs/已完成指标/第五章\" && mkdir -p \"docs/已完成指标/质控指标\" && mkdir -p \"docs/表结构参考\" && mkdir -p \"sql\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && ls -R --format=single-column)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && diff AGENTS.md CLAUDE.md)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && rm -rf \"docs/已完成指标/第一章\" \"docs/已完成指标/第二章\" \"docs/已完成指标/第三章\" \"docs/已完成指标/第四章\" \"docs/已完成指标/第五章\" \"docs/已完成指标/质控指标\")", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git status)", "Bash(cd \"C:\\Users\\leon\\AppData\\Roaming\\reasonix\\projects\\垂杨柳等评项目\" && git add . && git commit -m \"整理项目结构：docs 目录 + AGENTS.md 补充 Memory 学习机制\")"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit/move_file)
# may only write under workspace_root (empty = current dir) and allow_write extras.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
